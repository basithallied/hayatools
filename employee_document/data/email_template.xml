<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="email_template_document_status_change" model="mail.template">
            <field name="name">Document Status Change Notification</field>
            <field name="model_id" ref="model_document_tracking"/>
            <field name="subject">Document Status Update: ${object.document_type_id.name} - ${object.employee_id.name}</field>
            <field name="auto_delete" eval="True"/>
        </record>
    </data>
</odoo>