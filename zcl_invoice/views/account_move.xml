<odoo>
      <record id="view_invoice_inherit_form" model="ir.ui.view">
            <field name="name">account.move.form.inherited</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='journal_id']" position="after">
                    <field name="manuel_entry" invisible="1"/>
                </xpath>
                <xpath expr="//button[@name='action_register_payment']" position="before">
                    <button name="print_custom_invoice_report"
                            type="object"
                            string="Print Preview"
                            invisible = "move_type != 'out_invoice'"
                            />
                </xpath>

            </field>
     </record>
</odoo>