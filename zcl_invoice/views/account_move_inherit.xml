<odoo>
  <data>

        <record id="action_move_journal_lines_id" model="ir.actions.act_window">
      <field name="name">Journal Entries</field>
      <field name="res_model">account.move</field>
      <field name="view_mode">form,list</field> <!-- Opening the form view to create a new entry -->
      <field name="view_id" ref="account.view_move_tree"/>
      <field name="search_view_id" ref="account.view_account_move_filter"/>
      <field name="context">{'default_move_type': 'entry', 'default_state': 'draft', 'default_manuel_entry': True}
      </field>
      <field name="domain">[('manuel_entry', '=', True)]</field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
          Create a journal entry
        </p>
        <p>
          A journal entry consists of several journal items, each of
          which is either a debit or a credit transaction.
        </p>
      </field>
    </record>

<!--
    <record model="ir.ui.view" id="zcl_invoice/.list">
      <field name="name">zcl_invoice/ list</field>
      <field name="model">zcl_invoice/.zcl_invoice/</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </list>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="zcl_invoice/.action_window">
      <field name="name">zcl_invoice/ window</field>
      <field name="res_model">zcl_invoice/.zcl_invoice/</field>
      <field name="view_mode">list,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="zcl_invoice/.action_server">
      <field name="name">zcl_invoice/ server</field>
      <field name="model_id" ref="model_zcl_invoice/_zcl_invoice/"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "list,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="zcl_invoice/" id="zcl_invoice/.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="zcl_invoice/.menu_1" parent="zcl_invoice/.menu_root"/>
    <menuitem name="Menu 2" id="zcl_invoice/.menu_2" parent="zcl_invoice/.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="zcl_invoice/.menu_1_list" parent="zcl_invoice/.menu_1"
              action="zcl_invoice/.action_window"/>
    <menuitem name="Server to list" id="zcl_invoice/" parent="zcl_invoice/.menu_2"
              action="zcl_invoice/.action_server"/>
-->
  </data>
</odoo>
