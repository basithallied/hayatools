<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_stock_location_form" model="ir.ui.view">
        <field name="name">stock.location.form</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='location_id']" position='after'>
                <br/>
                <label for="supervisor_id"/>
                <h2><field name="supervisor_id" placeholder="Supervisor"/></h2>
                <label for="picker_ids"/>
                <h2><field name="picker_ids" placeholder="Pickers" widget="many2many_tags"/></h2>
            </xpath>
        </field>
    </record>

</odoo>
